// Settings services are per-user, stored in Firestore under users/{uid}
// See src/lib/firebase/auth.ts for getUserProfile, saveUserProfile,
// getUserNotifications, saveUserNotifications, getUserBilling, saveUserBilling
